pipeline {
    agent {label:masternodes}
    stages {
        stage('Ansible Playbook') {
        agent {label:node2}
            steps {
                ansiblePlaybook(
                    playbook: '/etc/opt/playbook/prafull.yml'
                    extras: '-e "myvar=myval"'
                )
            }
        }
    }
}

